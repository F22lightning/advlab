คำอธิบายโปรแกรม
1. การนำเข้าไลบรารี (import yt_dlp)
เป็นการเรียกใช้งานโมดูล yt_dlp เพื่อให้เราสามารถใช้คำสั่งดาวน์โหลดวิดีโอในโค้ดได้
2. ส่วนรับข้อมูล (input)
url = input(...): โปรแกรมจะหยุดรอให้คุณคัดลอกลิงก์ YouTube มาวางในหน้าจอ Terminal หรือ Console เพื่อเก็บไว้ในตัวแปร url
3. ส่วนกำหนดค่า (ydl_opts)
ตรงนี้คือ "การตั้งค่า" ก่อนโหลด:
'format': 'best': สั่งให้เลือกวิดีโอไฟล์เดียวที่มีความละเอียดดีที่สุด (ปกติจะได้ไฟล์ .mp4 หรือ .webm)
'outtmpl': '%(title)s.%(ext)s': กำหนดชื่อไฟล์ผลลัพธ์ โดยให้เอา ชื่อคลิป (title) ตามด้วย นามสกุลไฟล์ (ext)
4. ส่วนการดาวน์โหลด (try...except)
try:: โปรแกรมพยายามเริ่มดาวน์โหลด หากทำสำเร็จจะแสดงคำว่า "ดาวน์โหลดเสร็จสมบูรณ์!"
with yt_dlp.YoutubeDL(ydl_opts) as ydl:: เป็นการเปิดใช้งานเครื่องมือดาวน์โหลดพร้อมกับการตั้งค่าที่เรากำหนดไว้
ydl.download([url]): คือคำสั่ง "กดปุ่มเริ่มโหลด" โดยส่งลิงก์ที่เราวางไว้ให้โปรแกรมจัดการ
except Exception as e:: หากเกิดปัญหา (เช่น เน็ตหลุด, ลิงก์เสีย, หรือวิดีโอติดลิขสิทธิ์) โปรแกรมจะไม่ค้าง แต่จะแจ้งข้อความข้อผิดพลาดออกมาแทน

ควรติดตั้ง**pip install yt-dlp**

YouTube Video Downloader (Python) 🎥
โปรแกรมดาวน์โหลดวิดีโอจาก YouTube แบบเรียบง่ายผ่าน Command Line โดยใช้ไลบรารี yt-dlp

🛠 สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)
ก่อนจะรันโปรแกรมนี้ คุณต้องมีสิ่งต่อไปนี้ติดตั้งในเครื่องก่อน:

Python 3.x

pip (ตัวจัดการแพ็กเกจของ Python)

FFmpeg (แนะนำให้ติดตั้งเพื่อให้โปรแกรมสามารถรวมไฟล์วิดีโอและเสียงที่มีความละเอียดสูงได้)

🚀 วิธีการติดตั้ง (Installation)
Clone โปรเจกต์นี้ลงมาที่เครื่อง:

Bash
git clone https://github.com/your-username/your-repo-name.git
เข้าไปที่โฟลเดอร์ของโปรเจกต์:

Bash
cd your-repo-name
ติดตั้งไลบรารีที่จำเป็น:

Bash
pip install yt-dlp
💻 วิธีใช้งาน (Usage)
รันโปรแกรมด้วยคำสั่ง:

Bash
python main.py
(หมายเหตุ: เปลี่ยน main.py เป็นชื่อไฟล์ที่คุณตั้งไว้)

วางลิงก์ YouTube ที่คุณต้องการดาวน์โหลดในช่องรับข้อมูล

รอจนกว่าโปรแกรมจะขึ้นว่า ดาวน์โหลดเสร็จสมบูรณ์! ไฟล์วิดีโอจะปรากฏอยู่ในโฟลเดอร์เดียวกับตัวโปรแกรม

⚙️ การตั้งค่า (Configuration)
คุณสามารถแก้ไขไฟล์เพื่อเปลี่ยนรูปแบบการดาวน์โหลดได้ในตัวแปร ydl_opts:

format: เลือกคุณภาพ (เช่น 'best', 'bestvideo+bestaudio')

outtmpl: รูปแบบชื่อไฟล์ที่ต้องการบันทึก
